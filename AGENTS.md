# Agent Instructions

This repository is the personal GitHub Pages portfolio site for Kyaw Min Thu.
Full working instructions live in `CLAUDE.md` — read that first; it is the
single source of agent guidance for this repo.

Key constraints:

- The live site is the root `index.html` (static, no build step, Jekyll disabled).
- `RAG/` is private, gitignored career source data — never track or publish it.
- Track meaningful changes in `project_state/` (see CLAUDE.md for file ownership).
- Feature branches + conventional commits; never commit directly to `main`.
