[project]
name = "kyawminthu-tools"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "python-docx",
    "reportlab>=4.4.10",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[dependency-groups]
dev = [
    "pytest",
]
